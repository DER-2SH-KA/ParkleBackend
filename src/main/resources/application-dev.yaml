spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/start_page_test_db
    username: cock
    password: cockMaster

  security:
    bcrypt:
      strength: 10

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always
jwt:
  secret-key: a_very_long_and_complex_secret_key_that_is_at_least_256_bits_long_and_base64_encoded_or_just_a_long_random_string_like_this_example_but_longer_and_random
  expiration: 86400000
