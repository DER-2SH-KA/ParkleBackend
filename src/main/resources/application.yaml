spring:
  application:
    name: Parkle Project Jun 2025
  # profiles:
    # active: @activatedProperties@

  jpa:
    hibernate:
      ddl-auto: update

  liquibase:
    enabled: false
springdoc:
  api-docs:
    path: /api-docs
