spring:
  datasource:
    url: jdbc:postgresql://postgres_parkle:5432/${POSTGRES_DB}
    username: ${POSTGRES_USER}
    password: ${POSTGRES_PASSWORD}

  security:
    bcrypt:
      strength: ${SPRING_SECURITY_BCRYPT_STRENGTH}